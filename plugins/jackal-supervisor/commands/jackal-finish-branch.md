---
description: Finish the current branch — rebase if behind, push, open a PR, update the issue
---

# Jackal Finish Branch

Invoke the `jackal-finish-branch` skill for the current branch (or `$1` if a
branch/issue is given).
